import styled from 'styled-components';

const MaterialCardContainer = ({ className }) => {
	return (
		<div className={className}>
			<div>123</div>
		</div>
	);
};

export const MaterialCard = styled(MaterialCardContainer)``;
